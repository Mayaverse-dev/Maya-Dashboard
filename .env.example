# Required
METRICS_PORTAL_PASSWORD=change-me
SHARED_JWT_SECRET=change-me-too

# Local dev / Railway DB connection string
DATABASE_URL=postgresql://localhost:5432/maya_db

# For shared SSO-style cookie across subdomains (set in Railway).
# Example: .entermaya.com
COOKIE_DOMAIN=

# Optional (default: 7 days)
JWT_TTL_SECONDS=604800

